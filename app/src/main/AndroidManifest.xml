<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.abertay.forbes.assessment">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cpt"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".OpenScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ReadOptions"></activity>

        <activity android:name=".ReadingLog"></activity>

        <activity android:name=".RecordOptions"></activity>

        <activity android:name=".RecordingActive"></activity>

    </application>

</manifest>